/*// 1. Show a popup box to get the input string
const input = prompt(
  "Enter your age, height, grade, and name (space separated):",
);

// 2. Make sure the user didn't hit 'Cancel'
if (input) {
  // 3. Split the single string into an array using spaces
  const [age, height, grade, name] = input.split(" ");

  // 4. Print the results to your browser console
  console.log(`Your age: ${age}`);
  console.log(`Your height: ${height}`);
  console.log(`Your grade: ${grade}`);
  console.log(`Your name: ${name}`);
} else {
  console.log("No input provided.");
}*/
