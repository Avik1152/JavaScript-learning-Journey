# DSA with JavaScript

DSA stands for Data Structures and Algorithms. Data structures manage how data is stored and accessed. Algorithms focus on processing this data.

* Example:
  * example of data structures are `Array`, `Linked List`, `Tree` and `Heap` and
  * example of algorithms are `Binary Search`, `Quick Sort` and `Merge Sort`.
